<button type="primary" bindtap="onGetUsrProfile" wx:if="{{showLogOnButton}}"> 登录 </button>
<view class="afterLogOn" wx:else>
    <image  src="{{userInfo.avatarUrl}}" class="userAvatar"/>
    <text class="userNickName">{{userInfo.nickName}}</text>
</view>
